name: Keep Alive
on:
  schedule:
    - cron: '*/4 * * * *'   # 每4分钟访问一次，防止函数休眠
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping site
        run: curl -s -o /dev/null -w "%{http_code}" https://daydream7.pages.dev/api/menu
